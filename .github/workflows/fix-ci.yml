name: Fix CI

# This workflow previously applied CI fixes.
# Fixes have been applied — this file is kept for reference.
# To re-run, use workflow_dispatch.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Fixes already applied. This workflow is kept for reference only."
